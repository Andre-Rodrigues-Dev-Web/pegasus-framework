import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pegasus.mobilenav',
  appName: 'PegasusMobile',
  webDir: 'dist/template-with-navigation-mobile'
};

export default config;
