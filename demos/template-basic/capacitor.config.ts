import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pegasus.basic',
  appName: 'PegasusBasic',
  webDir: 'dist/template-basic'
};

export default config;
